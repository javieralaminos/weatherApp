export enum WeatherType {
  temperature = 'temperature',
  humidity = 'humidity',
  pressure = 'pressure',
}

export enum AverageType {
  daily = 'daily',
  hourly = 'hourly',
  minutely = 'minutely',
}